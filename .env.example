# Copy this to .env.local for local dev (never commit .env.local)
# Add these same variables to Vercel: Settings → Environment Variables

# Supabase — get these from supabase.com → your project → Settings → API
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key-here

# Legacy env-var auth (kept for fallback, no longer used for login)
# ADMIN_EMAIL=your@email.com
# ADMIN_PASSWORD=your-password-here
# SESSION_SECRET=replace-with-a-random-64-char-hex-string
